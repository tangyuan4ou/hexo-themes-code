@import './../_variables.styl';

.main {
  position: relative;
  width: 100%;
  min-width: 1200px;
  overflow: hidden;
}
.indexContainer {
  position: relative;
  width: 100%;
}

//标题
.t_wrap {
    position: absolute;
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    width: 1200px;
  .title {
    font-family: 'Clicker Script';
    text-align: center;
    font-size: 100px;
    font-weight: bold;
    color: #000000;
  }
}

